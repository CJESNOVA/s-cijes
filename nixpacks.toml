providers = []

[variables]
PHP_EXTENSIONS = ""
NIXPACKS_NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["php", "nodejs_20", "nginx", "phpPackages.composer"]

# Étape manquante : On définit la commande de démarrage
[start]
cmd = "nginx -c /etc/nginx/nginx.conf"