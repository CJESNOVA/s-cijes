providers = []

[variables]
PHP_EXTENSIONS = ""
NIXPACKS_NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["php", "nodejs_20", "nginx", "phpPackages.composer"]

[start]
# On lance PHP-FPM en arrière-plan (-D), puis Nginx au premier plan
cmd = "php-fpm -D && nginx -g 'daemon off;'"