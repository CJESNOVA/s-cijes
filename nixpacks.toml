# On liste les providers sous forme de tableau (séquence)
providers = ["php", "node"]

[variables]
# Évite le plantage sur la détection automatique des extensions complexes
PHP_EXTENSIONS = ""

[phases.setup]
# Versions stables pour ton environnement
nixPkgs = ["php", "nodejs_20", "nginx", "phpPackages.composer"]